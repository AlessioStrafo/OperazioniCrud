spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/devdb
    username: root
    password: root
  jpa:
    database: mysql
    show_sql: true
    hibernate:
      ddl-auto: update
      application:
        name: esercizio-crud
        jackson:
          serialization:
            fail-on-empty-beans: false

